<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Master Big Data - Auth{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <style>
            body { background-color: #f8f9fa; }
            .navbar { margin-bottom: 2rem; }
        </style>
        {% block stylesheets %}{% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {# Barre de navigation visible uniquement si connecté #}
        {% if app.user %}
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
                <div class="container">
                    <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">🔒 Sécurité TP1</a>
                    <div class="ms-auto">
                        <span class="text-light me-3 small">Connecté : <strong>{{ app.user.identifiant }}</strong></span>
                        <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm">Déconnexion</a>
                    </div>
                </div>
            </nav>
        {% endif %}

        <div class="container">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>